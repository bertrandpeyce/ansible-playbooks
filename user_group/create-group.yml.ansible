---
- name: Create group
  hosts: all
  become: true
  tasks:
    - block:
      - name: Create group "{{ group_name }}"
        ansible.builtin.group:
          name: "{{ group_name }}"
          state: present
