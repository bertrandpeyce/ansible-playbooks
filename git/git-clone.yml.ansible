---
- name: Clone a git repo
  hosts: all
  tasks:
    - name: Clone a git repo
      ansible.builtin.git:
        repo: "{{ git_repo }}"
        dest: "{{ git_repo_dest }}"
        version: "{{ git_repo_version }}"
        single_branch: yes

